- @title = @user.name

.row
  .column.small-2
    = image_tag "avatars/#{ @user.avatar }", :alt => @user.name
  .column.small-10
    %h1
      = @user.name	
      - if @user.id == current_user.id
        %small (That's you!)
