(function($){
	
	var newComment = '<%= escape_javascript(render :partial => "shared/comment", :object => @comment) %>',
		$comments = $('#comments'),
		$textarea = $('form textarea');
		
	$comments.append(newComment);
	$(newComment).highlight();
	$textarea.val('');
	
}(jQuery));